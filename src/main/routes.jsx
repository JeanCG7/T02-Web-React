import React from 'react'
import { Router, Route, Redirect, hasHistory} from 'react-router'

export default props => (
    <Router history={hasHistory}>
        {/* <Route path='/' component={Tod}/> */}
    </Router>
)